{% extends 'base.html.twig' %}

{% block title %} {{ product.name }} {% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Page ! </h1>


     <img src="{{ asset(product.image) }}" alt="">
     <h2 style="color: #aacd4e; background-color: black; padding-left: 9px; margin-bottom: 0 ">{{ product.name }} < Nul ce jeu !!! </h2>
     <p style="border: 3px black solid; padding: 50px; margin: 0">{{ product.description }}</p>
     <p> Crée le {{ product.creationDate | date('d/m/Y') }}</p>




{#
            <a href="{{ path('home') }}">Back to home page</a>
#}

</div>
{% endblock %}
